/**
 * 8kyu - Basic Variable Assignment
 *
 * Expected output: "codewa.rs"
 */
export function basic_variable_assignment(): string {
  const a: string = 'code';
  const b: string = 'wa.rs';
  const name: string = a + b;

  return name;
}
