/**
 * 8kyu - Basic Variable Assignment
 *
 * Expected output: "codewa.rs"
 */
export function basic_variable_assignment() {
  const a = 'code';
  const b = 'wa.rs';
  const name = a + b;

  return name;
}
